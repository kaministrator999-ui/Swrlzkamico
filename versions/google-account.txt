ID=google-account
VERSION=1.0.4
KIND=account-auth-architecture
SOURCE_BRANCH=mixed
STATUS=active
DISPLAY_NAME=Google Account Architecture
UPDATE_POLICY=runtime-ui/stable-auth-boundary
BASELINE_NOTE=Chat history and preferences now use separate browser storage namespaces selected by the signed-in Google account subject, with one-time legacy migration and account-switch reload. OAuth configuration and credential tokens remain hidden. Durable cross-device account sync still requires server-side Google verification plus persistent account storage.
