ID=google-account
VERSION=1.0.5
KIND=account-auth-architecture
SOURCE_BRANCH=mixed
STATUS=active
DISPLAY_NAME=Google Account Architecture
UPDATE_POLICY=runtime-ui/stable-auth-boundary
BASELINE_NOTE=Chat now reads the public Google OAuth client ID from the server account-status authority and mirrors it into browser-local compatibility storage, eliminating per-browser test-page setup once server auth is configured. Server-side Google verification and durable account storage remain the authoritative stable boundary.
