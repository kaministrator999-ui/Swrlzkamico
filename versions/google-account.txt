ID=google-account
VERSION=1.0.8
KIND=account-auth-architecture
SOURCE_BRANCH=mixed
STATUS=active
DISPLAY_NAME=Google Account Architecture
UPDATE_POLICY=runtime-ui/stable-auth-boundary
BASELINE_NOTE=Google sign-in repairs empty or specifically retired browser OAuth state synchronously before the Chat account footer initializes Google Identity Services, preventing the prior boot-order race. Any non-retired browser-proven OAuth Web Client ID remains preserved. Server-side Google ID-token verification and signed HttpOnly account sessions remain authoritative after credential issuance; durable profile/chat sync still requires the Redis REST boundary.
