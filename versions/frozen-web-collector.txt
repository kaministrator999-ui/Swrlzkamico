ID=frozen-web-collector
VERSION=1.0.1
KIND=runtime-service
SOURCE_BRANCH=runtime
STATUS=active
DISPLAY_NAME=§wyrlz Frozen Web Snapshot Collector
UPDATE_POLICY=runtime-hot-compatible
API_SCHEMA=1
STATE_SCHEMA=1
BASELINE_NOTE=Collector 1.0.1 preserves API/state schema 1 and repairs durable state overwrites when an existing private Blob read does not expose a usable ETag. Existing-state fallback re-reads current revision before overwrite and verifies the exact persisted state after writing; the compatibility wrapper pins and Git-blob-verifies the 1.0.0 implementation source before applying only the state-save correction.
