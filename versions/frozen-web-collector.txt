ID=frozen-web-collector
VERSION=1.0.9
KIND=runtime-service
SOURCE_BRANCH=runtime
STATUS=active
DISPLAY_NAME=§wyrlz Frozen Web Snapshot Collector
UPDATE_POLICY=runtime-hot-compatible
API_SCHEMA=1
STATE_SCHEMA=1
BASELINE_NOTE=Collector 1.0.9 adds automatic source crawl profiles. Wikipedia portal seeds normalize to English Wikipedia Main Page, Wikipedia crawling stays on en.wikipedia.org main-article /wiki/ URLs, known non-content namespaces and query variants are filtered before frontier expansion, stale off-profile frontier items are rejected before fetch, and Wikipedia-only snapshots automatically disable external-domain exploration. Exact content deduplication, quality gates, robots compliance, immutable snapshots, and explicit training review remain unchanged. Collector 1.0.8 binds mutable state to the Blob metadata ETag across a cache-bypassed read, preserves conditional writes, and rejects missing write versions. Collector 1.0.7 preserves conditional-write failures while exposing bounded, credential-redacted provider errors for authenticated diagnosis; no unconditional overwrite fallback is added. Collector 1.0.6 fixes the response-limit configuration input's decimal MiB step and exact byte-equivalent bounds; the engine and saved data remain unchanged. Collector 1.0.5 fixes Start new revision continuity directly in the canonical runtime source. _new_snapshot_state now inherits the previous durable stateRevision before _save_state advances it, so a new snapshot no longer resets to revision zero and falsely conflicts with an existing Vercel Blob state when no usable ETag is returned. Collector 1.0.4 remains the direct missing-ETag writer repair; failed 1.0.1 and 1.0.2 wrapper attempts and 1.0.3 recovery remain preserved in lineage. No API/state schema or durable collector data is reset.
