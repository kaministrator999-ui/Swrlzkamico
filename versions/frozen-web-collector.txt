ID=frozen-web-collector
VERSION=1.0.5
KIND=runtime-service
SOURCE_BRANCH=runtime
STATUS=active
DISPLAY_NAME=§wyrlz Frozen Web Snapshot Collector
UPDATE_POLICY=runtime-hot-compatible
API_SCHEMA=1
STATE_SCHEMA=1
BASELINE_NOTE=Collector 1.0.5 fixes Start new revision continuity directly in the canonical runtime source. _new_snapshot_state now inherits the previous durable stateRevision before _save_state advances it, so a new snapshot no longer resets to revision zero and falsely conflicts with an existing Vercel Blob state when no usable ETag is returned. Collector 1.0.4 remains the direct missing-ETag writer repair; failed 1.0.1 and 1.0.2 wrapper attempts and 1.0.3 recovery remain preserved in lineage. No API/state schema or durable collector data is reset.
