ID=frozen-web-collector
VERSION=1.0.2
KIND=runtime-service
SOURCE_BRANCH=runtime
STATUS=active
DISPLAY_NAME=§wyrlz Frozen Web Snapshot Collector
UPDATE_POLICY=runtime-hot-compatible
API_SCHEMA=1
STATE_SCHEMA=1
BASELINE_NOTE=Collector 1.0.2 preserves API/state schema 1 and replaces failed 1.0.1 wrapper import. It executes the immutable pinned 1.0.0 collector source directly, then overrides only durable state saving. Missing ETag no longer implies a new object: existing state is re-read and revision-checked before overwrite, then the exact canonical persisted state is verified after the write. Failed Collector 1.0.1 remains preserved in release lineage.
