ID=frozen-web-collector
VERSION=1.0.3
KIND=runtime-service
SOURCE_BRANCH=runtime
STATUS=active
DISPLAY_NAME=§wyrlz Frozen Web Snapshot Collector
UPDATE_POLICY=runtime-hot-compatible
API_SCHEMA=1
STATE_SCHEMA=1
BASELINE_NOTE=Collector 1.0.3 restores the last verified working 1.0.0 runtime implementation after live import failures in 1.0.1 and 1.0.2 compatibility-wrapper attempts. This recovery restores readiness without changing API/state schema or durable collector data. The original false STATE_WRITE_CONFLICT on existing state without a usable ETag remains unresolved and requires a subsequent direct-source or stable-host repair; failed 1.0.1 and 1.0.2 remain preserved in lineage.
