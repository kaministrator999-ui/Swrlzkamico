# §wyrlz manual production deployment request
# Explicit one-retry approval granted after run #17 failed pre-deploy verification import.

TARGET=production
APPROVED=1
REQUEST_ID=2026-09-19-r39-native-pythonpath-retry-v1
REQUESTED_AT=2026-09-19T00:00:00Z
REQUESTED_BY=ChatGPT-explicit-retry-approval
SOURCE_REF=main
REASON=One approved retry after run #17 proved both native OpenMP extensions compile successfully but verification could not import the repository package. Workflow now sets PYTHONPATH to GITHUB_WORKSPACE for native verification and diagnostics; artifact and live gates remain mandatory.
