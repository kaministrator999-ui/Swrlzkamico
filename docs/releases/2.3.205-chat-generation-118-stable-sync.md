# Server 2.3.205 / Chat 1.5.55 — generation 118 stable/runtime synchronization

## Purpose
Reconcile the production-stable loader contract with the runtime Chat fixes accumulated during the Android boot-hang investigation, without discarding newer canonical Redis or sigil work on `main`.

## Runtime generation
`runtime_pages/manifest.json` advances from v117 to v118. This is a new immutable asset identity, so browsers must not reuse v117 JS/CSS for the new candidate.

The `/chat` manifest now injects only:
1. `chat_early_shell_ready_v1.js`
2. `chat_runtime_loader_v3.js`

The cooperative runtime loader is the single owner of the remaining functional/decorative graph. This removes duplicate direct manifest injection of background resume, account sync, canonical reconciliation, activity/sigil, language, and online-research helpers while the loader is also hydrating Chat.

## Stable loader change prepared on main
`api/live_source_guard.py` now emits manifest bootstrap scripts with `defer`. Manifest order is preserved, but runtime augmentation cannot block parsing of the base Chat document. This stable change is committed to `main` and intentionally not deployed by this release record; production activation waits for the user-controlled deployment.

## Preserved main lineage
The newer `main` commits after the currently deployed production SHA are retained, including the canonical Redis turn-status endpoint and §wyrlz sigil documentation. No rollback of those changes is performed.

## Versions
- Server Runtime: 2.3.205
- Web Chat: 1.5.55
- Runtime manifest: 118

## Deployment state
- Runtime branch changes: committed and hot-readable.
- Stable `main` loader correction: committed, pending user deployment.
- Vercel production deployment: NOT performed.
