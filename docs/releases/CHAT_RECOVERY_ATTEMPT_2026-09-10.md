# Chat recovery attempt — 2026-09-10

The runtime branch remains the hot Chat source. This checkpoint loads the existing Chat enhancement layer through `web/chat_account.js` after the base Chat page initializes.

Recovery branch preserved separately: `chat-recovery-2026-09-09`.

Runtime integration commit follows the prior runtime head and is intended to be reversible without touching `main`.
